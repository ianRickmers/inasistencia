spring:
  application:
    name: inasistencia-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8180
  datasource:
    url: jdbc:mysql://localhost:3306/bdinasistencias
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update  